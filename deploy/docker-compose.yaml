version: '3.8'
services:
  frontend:
    #build: ../frontend
    ports:
      - "3000:3000"
    env_file:
      - ../frontend/.env

  backend:
    #build: ../backend
    ports:
      - "4000:4000"
    env_file:
      - ../backend/.env